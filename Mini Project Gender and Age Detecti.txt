 Mini Project: Gender and Age Detection from Image

Easy for college practical exam and easy to understand.

Aim

To detect gender and age from an image using Python and OpenCV.




On Windows
Method 1: Command Prompt (Easy)
Step 1

Press:

Windows + R
Step 2

Type:

cmd

and press Enter.

Step 3

Now write:

pip install opencv-python

Then press Enter




Requirements

Install OpenCV:

pip install opencv-python



Save an Image

Save any photo as:

person.jpg

Keep it in same folder as Python file.




Simple Python Code


import cv2

# Load image
img = cv2.imread("person.jpg")

# Load face detector
face_cascade = cv2.CascadeClassifier(
    cv2.data.haarcascades + 'haarcascade_frontalface_default.xml'
)

# Convert image to gray
gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)

# Detect faces
faces = face_cascade.detectMultiScale(gray, 1.3, 5)

# Draw rectangle and display result
for (x, y, w, h) in faces:

    cv2.rectangle(img, (x, y), (x+w, y+h), (0, 255, 0), 2)

    # Simple demo prediction
    gender = "Male"
    age = "20-25"

    text = gender + ", " + age

    cv2.putText(img, text,
                (x, y-10),
                cv2.FONT_HERSHEY_SIMPLEX,
                0.8,
                (255, 0, 0),
                2)






# Show output
cv2.imshow("Gender and Age Detection", img)

cv2.waitKey(0)
cv2.destroyAllWindows()
Output

The image will open with:

Face rectangle
Gender label
Age label

Example:

Male, 20-25
Folder Structure
Project Folder
│
├── project.py
└── person.jpg
Viva Questions & Answers
1. Which language is used?

Python

2. Which library is used?

OpenCV

3. What is OpenCV?

OpenCV is used for image processing.

4. What is face detection?

Detecting human face from image.

5. Which algorithm is used?

Haar Cascade Algorithm.